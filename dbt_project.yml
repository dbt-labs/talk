name: 'talk'
version: '2.0'

profile: 'talk'

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

quoting:
  identifier: false

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"

models:
  talk:
    materialized: view
